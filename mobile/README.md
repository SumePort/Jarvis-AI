# DOOM Mobile Client

The mobile client is a thin authenticated DOOM device node. It uses the same versioned protocol, advertises capabilities, maintains heartbeats, and never stores protected vault material. Flutter UI and native background adapters can sit above this protocol.
