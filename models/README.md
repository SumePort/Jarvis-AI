# Local Models

Jarvis does not bundle model weights.

Use a small CPU-compatible quantized GGUF model with an OpenAI-compatible llama-server.

Development model:
E:\\ULTRON\\models\\llm\\qwen2.5-0.5b-instruct-q4_k_m.gguf

Keep model files outside Git unless you intentionally want to version large model artifacts.
