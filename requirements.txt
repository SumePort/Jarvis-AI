# Jarvis AI — local-first runtime
requests>=2.32,<3
python-dotenv>=1.1,<2
psutil>=7,<8
PyAutoGUI>=0.9.54,<1
PyGetWindow>=0.0.9,<1
pywin32>=311,<312
pyperclip>=1.9,<2
pytest>=8,<9

# LiveKit realtime voice
livekit-agents[openai]~=1.5

# Optional legacy local voice / OCR
# sounddevice
# vosk
# pytesseract
# Pillow

# Optional Piper environment:
# PIPER_EXE=...
# PIPER_MODEL=...

# DOOM protected vault
cryptography>=44,<46
